version: 2

sources:
  - name: jaffle_shop
    database: raw
    schema: stripe
    freshness:
          warn_after:
            count: 78
            period: hour
          error_after:
            count: 78
            period: hour
    loaded_at_field: _batched_at
    tables:
      - name: payment

