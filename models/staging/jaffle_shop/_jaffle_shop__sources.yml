version: 2

sources:
  - name: jaffle_shop
    database: raw
    schema: jaffle_shop
    freshness:
          warn_after:
            count: 78
            period: hour
          error_after:
            count: 78
            period: hour
    loaded_at_field: _etl_loaded_at
    tables:
      - name: customers
        columns:
          - name: id
            tests:
               - not_null
               - unique
        freshness: null

        
      - name: orders
